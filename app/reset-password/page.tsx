'use client';
import {useState} from 'react';
import {browserDb,DEMO} from '@/lib/supabase';
export default function ResetPassword(){const[password,setPassword]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);return <main className="orders-section" style={{maxWidth:500}}><a className="brand" href="/">PLYDECK.</a><h1 style={{marginTop:35,fontSize:36}}>Set a new password.</h1><form className="form-stack" onSubmit={async e=>{e.preventDefault();setBusy(true);try{if(DEMO)throw new Error('Password reset is unavailable in demo mode.');const{error}=await browserDb().auth.updateUser({password});if(error)throw error;setMessage('Password updated. Return to PLYDECK to continue.');}catch(e){setMessage((e as Error).message);}finally{setBusy(false);}}}><label>New password<input type="password" autoComplete="new-password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button dark" disabled={busy}>{busy?'Saving…':'Update password'}</button><p role="status">{message}</p><a href="/">Return to PLYDECK</a></form></main>;}
